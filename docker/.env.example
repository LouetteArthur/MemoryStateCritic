# Copy to docker/.env and fill in. This file is committed as the template;
# docker/.env is gitignored.

# Optional: override the CUDA base image used at build time.
# Defaults to nvcr.io/nvidia/cuda:12.8.0-cudnn-devel-ubuntu22.04
# CUDA_IMAGE=nvcr.io/nvidia/cuda:12.8.0-cudnn-devel-ubuntu22.04

# Weights & Biases
WANDB_API_KEY=
WANDB_ENTITY=
WANDB_PROJECT=
